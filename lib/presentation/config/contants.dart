import 'package:flutter/animation.dart';

class AppConstants {
  static const fontGraphik = 'Graphik';

  /// Duration between animation slides in milliSeconds.
  static const pageControllerAnimationDuration = 500;

  /// Animation curve used for animating between slides.
  static const pageControllerAnimationCurve = Curves.ease;
}
